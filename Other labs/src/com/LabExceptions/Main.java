package com.LabExceptions;

import com.LabExceptions.PatternMatcher.*;
import com.LabExceptions.DriveExplorer.*;
import com.LabExceptions.SortArray.*;

public class Main {
    public static void main(String[] args) {
	    new DEApplication().run();
	    new PMApplication().run();
	    new SAApplication().run();
    }
}
